# 2026-06-06

- 安装了 taste-skill 插件（来自 https://github.com/Leonxlnx/taste-skill），13 个设计品味 skill 已配置到 `external_plugins/taste-skill/`
- 整理了全部可用 skills 清单（~200+ 个）写入 MEMORY.md，按来源/类别分为 9 大类：项目级、用户级、Manager、设计前端、游戏开发、浏览器自动化、安全文件、工具、external_plugins 内置专业 skills
- **项目从 C 盘迁移到 E 盘**：修复了 `run.bat`、`install.bat`、`GitHub.bat` 三个脚本中的硬编码路径，`C:\Users\13601\Desktop\Super\super-mahjong` → `E:\Desktop\Super\super-mahjong`。其余文件（package.json 等）无硬编码路径，无需修改。
- **部署 PocketMahjongClient**：从 `https://github.com/winktzhong/PocketMahjongClient` clone 到 `mahjong-game/`，Cocos Creator 3.7.4 日麻游戏客户端。已移除 .git 子仓库，`npm install --legacy-peer-deps` 安装 93 个依赖（解决 protobufjs 版本冲突）。
- **删除角色 tags 数据及关联代码**：移除 `data/characters.json` 中 76 个角色的 `tags` 属性。删除 `server.js` 第 835 行 tags 映射逻辑。删除 `components.js` 中 `renderStrategyTags()` 函数及所有调用。删除 `battle_round.html` 和 `game.html` 中所有 `.tags` 引用和 `strategy-tags` CSS。删除 `style.css` 中 12 种 `.strategy-tag.*` 样式（速攻/蓄力/防守/高打点/交换/控制/赌注/额外巡目/万能牌/封印/复制/决斗）。最终搜索确认零残留。
- **角色强度调整**：亦野诚子 T2→T1（三副露控场+一炮多响）、郝慧宇 T2→T1（规则免疫+低赔付）、梦乃真帆 T1→T2（复制技一次性依赖阵容）。调整后：T0×3 / T1×19 / T2×50 / T3×4。
- **JSON 数据同步到 xlsx**：将 `characters.json` 中 76 个角色全部数据（阵营/名称/Tier/技能/备注等）按角色名匹配写入 `super-mahjong.xlsx` 的"修改版"sheet，含 3 处强度调整。
- **创建双向同步脚本 `tools/sync_characters.py` + `sync.bat`**：按 B 列 ID（SRM0001-SRM0076）匹配，支持 xlsx→json（全量覆盖）和 json→xlsx（按ID更新）两个方向。名称校验防错。搭配 `sync.bat` 菜单入口方便使用。
- **JSON 补齐 `id` 字段**：`data/characters.json` 中 76 个角色增加 `"id": "SRM####"` 字段。
- **补充角色技能说明**：为 `super-mahjong.xlsx` 的"修改版"sheet 中全部 76 个角色的 note 列写入技能术语解释（如倒置、复合、筋牌、万能牌、暗置等），减少游玩争议。
