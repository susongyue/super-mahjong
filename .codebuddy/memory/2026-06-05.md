# 2026-06-05

- 修复 install.bat 中 npm 调用不加 `call` 导致不返回 menu 的问题
- 修复 battle_round.html 中角色详情点击重复触发导致"未找到角色数据"的问题（wrapper 和 card 双重绑定）
- 为所有 7 个 HTML 页面添加 favicon，先使用 AI 生成的 favicon.png，后改用春夏秋冬梅兰竹菊 SVG 合成，最终切换为 public/dong.png
- **性能优化**：
  - 73 张角色 PNG 转 WebP（25.3MB → 0.9MB，缩小 96.4%），输出到 public/webp/
  - server.js 添加 compression 中间件（Gzip 压缩）+ 图片缓存头（Cache-Control: max-age=31536000, immutable）
  - utils.js 图片加载改用 `<picture>` 标签 WebP 优先 + PNG 降级
  - 6 个页面添加 `<link rel="preconnect">` 加速 CDN（cdnjs）连接预热
  - GSAP 脚本保持同步加载（defer 会破坏内联 gsap.from() 调用）
