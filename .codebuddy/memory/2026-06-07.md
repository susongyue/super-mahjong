# 2026-06-07

- **创建角色图鉴页面 `public/encyclopedia.html`**：独立页面包含 CSV 上传功能（从 index.html 迁移）、角色网格展示（桌面6列/平板4列/手机3列）、Tier 筛选、名称搜索、点击查看详情弹窗（含技能说明note）。头像使用 `/webp/` 和 `/png/` 路径（与现有 charImageHTML 一致，WebP优先+PNG降级）。
- **从 `public/index.html` 移除 CSV 上传区域**：删除 `.admin-section` CSS、`#csvDrop` HTML、`uploadCSV`/`loadCharCount` JS 代码三部分。
- **全部 7 个页面导航栏新增"角色图鉴"链接**：lobby.html、index.html、game.html、battle_round.html、history.html、profile.html、encyclopedia.html — 放置于"对局记录"与"个人中心"之间，使用书本 SVG 图标。
- **上传方式从 CSV 改为 JSON**：`server.js` 新增 `/api/upload-json` 端点（接收 JSON 数组直接写入 `characters.json`），`api.js` 新增 `uploadJSON` 方法，`encyclopedia.html` 中上传区、文件接受类型、处理函数全部改为 JSON。

- **Impeccable UI 优化（基于 `pbakaus/impeccable` skill）**：
  - P0: 全站 8 个 HTML 移除 `user-scalable=no` + `maximum-scale=1`（恢复浏览器缩放）
  - P1: 新增 `:focus-visible` 键盘焦点样式、`prefers-reduced-motion: reduce` 关闭动画/blur、`pointer: coarse` 触摸目标 ≥44px
  - P2: spacing 对齐 4px 网格（15→16, 30→32）、body 字号 15→16px（line-height 1.6→1.65）
  - 色彩：canvas 从纯黑 `#0a0a0f` 改为品牌绿色调 `#0a0f0d`，所有 surface/hairline/avatar-bg/toast-bg 同步更新
  - 硬编码色值修复：`.fs-deploy-btn` 的 `#fbb400` → `var(--gold)`

- **详情弹窗统一 + 渐显效果**：将 game.html（选角）和 battle_round.html（出战）的全屏展开弹窗替换为 encyclopedia.html 的居中卡片式详情弹窗，GSAP 长动画替换为 CSS transition 渐显（0.25s opacity + scale）：
  - `style.css`：删除约165行 `.char-fullscreen-*`/`.fs-*` CSS，新增 `.detail-overlay`/`.detail-card` 系列样式（含 `pointer-events` 状态切换、`.show` 类控制渐显、`transform: scale(0.94→1)`、detail-footer/deploy-btn/retain 样式）
  - `encyclopedia.html`：移除内嵌 `.detail-*` CSS（迁移到 style.css），改用 `.show` 类触发渐显
  - `game.html`：HTML 替换为 `<div class="detail-overlay"><div class="detail-card">`，JS 简化为 innerHTML 构建（头像/picture-WebP/技能/说明/出战按钮/保留提示），移除 GSAP 动画、`fsAnimating`/`fsCurrentCard`/`fsCurrentIdx` 变量
  - `battle_round.html`：同样替换 HTML + JS，tier 行显示玩家名，retain 提示移入 `.detail-footer`
  - CSS 缓存版本 v5→v6（encyclopedia/game/battle_round 三页）
  - 清理陈旧 CSS 引用：删除了 `.char-fullscreen-overlay .fs-*` 在 responsive、border-width、focus-visible、reduced-motion 区域的所有残留规则

- **删除所有 font-weight 属性**：style.css 中 36 处 font-weight: 声明全部移除。
- **修复 3 个角色错误 note**：xlsx + characters.json 同步清空：
  - 梦乃真帆（索子赤宝牌 → 与复制技能无关）
  - 瑞原早璃（带手牌到下一局 → 与王牌交换技能无关）
  - 鹿仓胡桃（余点作废 → 通用规则非角色说明）

- **修复 df.to_excel() 副作用**：pandas 写回 xlsx 导致 sheet 名从 "修改版" 变为 "Sheet1"，`sync_characters.py` 因此崩溃。用 openpyxl 改回 "修改版"，并给脚本加 `_get_sheet()` 容错（找不到则用第一个 sheet）。

- **补全 3 个角色的 note**：
  - 梦乃真帆：复制技能一触即弃机制 + 每类别限一项
  - 鹿仓胡桃：牌河交换归属互换 + 固有技防副露/荣和
  - 瑞原早璃：王牌尾部定义 + 交换上限 + 被动复合番数叠加规则

- **统一技能术语 + 明确发动时机**（76个角色全量修改）：
  - 术语统一：其他家/他家/别家→其他玩家、任意家→任意玩家、立直家→立直玩家、荣和家→荣和玩家、胡牌家→胡牌玩家、副露家→副露玩家、收牌家→收牌玩家、喂牌家→喂牌玩家、每家→每名玩家、各家→各玩家、多家→多名玩家、对手→其他玩家
  - 时机明确化（11处）：
    - SRM0001 原村和：你可以倒置→你可以在舍牌时倒置
    - SRM0003 宫永咲：若此时→若你开杠时
    - SRM0008 井上纯：于你的巡目明确
    - SRM0011 泽村智纪：于舍牌时倒置
    - SRM0021 松实玄：听牌时→听牌状态下
    - SRM0025 小走八重：在舍牌前宣言
    - SRM0030 涩谷尧深：南4局→进入南4局时（巡目开始前）
    - SRM0051 薄墨初美：你的巡目→你的巡目摸牌前
    - SRM0055 小濑川白望：在你的巡目时→在你摸牌后
    - SRM0058 花田煌：你一向听时→你摸牌后一向听时
    - SRM0074 春日野穹：你的巡目→你摸牌后
    - SRM0075 小鸟游六花：摸牌时→摸牌前
  - xlsx同步待完成（Excel占用中，需关闭后运行`python tools/sync_characters.py --to-xlsx`）

- **xlsx 同步脚本格式保护加固**（`tools/sync_characters.py`）：
  - 分析 xlsx 格式：修改版 sheet、11列 A-K、header(微软雅黑/14/bold/白字/theme7填充)、data(微软雅黑/12/white fill)、列宽(A=15,B=10.125,C=15.625,D=5.625,E=27.375,J=17.625,K=18.5)、冻结A15、无边框、无条件格式
  - 新增 `_extract_template_style()`：从第一行数据行提取字体/填充/边框/对齐/数字格式模板（用 `copy.copy()` 避免弃用警告）
  - 新增 `_ensure_cell_format()`：对之前为空的单元格（默认 Calibri 字体）自动应用模板格式
  - `json_to_xlsx` 改进：只写 `.value` 不碰格式；写入前从已有行提取模板；K列（片假名）不在 JSON schema 中，永不覆盖
  - 新增行处理：JSON 比 xlsx 多的角色自动追加到末尾并用模板格式
  - 验证通过：76角色全部匹配，字体/填充/对齐/列宽/行高/冻结窗格完美保留

- **禁止页面右侧水平滚动条**：`public/style.css` 中给 `html` 和 `body` 都添加 `overflow-x: hidden`，防止任何页面出现右侧水平滚动条

- **CloudBase 部署**：将 super-mahjong 部署到腾讯云 CloudBase CloudRun 容器模式：
  - 创建 `Dockerfile`（Node 18 / 端口 3000 / npm start）和 `.dockerignore`
  - 环境：`sr-mahjong-d0g8mp6w19f37dd37`（上海 ap-shanghai）
  - CloudRun 服务名：`super-mahjong`，0.5核/1GB/1-5实例/公网
  - 在线地址：`https://super-mahjong-267217-4-1324905494.sh.run.tcloudbase.com`
  - 部署时间：2026-06-07 06:42
  - **部署修复（08:30）**：
    - 移除未使用的 `sharp` 依赖（C++ 编译拖慢构建）
    - Dockerfile 基础镜像从 `node:18-alpine` 升级到 `node:22-alpine`（Supabase @supabase/realtime-js 需要 Node 22+ 原生 WebSocket）
    - 当前版本：`super-mahjong-005`

- **login.html 设为首页索引 + 移除导航栏 + 移动端优化**（09:06）：
  - `server.js`：新增 `app.get('/')` 路由，访问根路径直接返回 `login.html`
  - `login.html`：移除顶部 `<nav class="navbar">` 导航栏，页面变为纯登录框
  - 移动端优化：`<480px` 下 input/btn 高度 48px、`font-size: 16px`（防 iOS 缩放）、`100dvh` 居中、`body padding: 12px`
  - GSAP 动画简化为仅登录框 fade-in

- **lobby.html 布局修复**（09:15）：
  - 根因：`.container` 的 `padding-top` 仅 12px，但固定导航栏高 56px，内容被遮挡导致布局错乱
  - `style.css`：全局 `.container` padding-top 从 12px → 72px（56px navbar + 16px spacing）
  - `lobby.html`：新增 lobby 专属 `.container { padding-top: 72px }` 强化覆盖
  - 移动端优化：<540px 下 form-row 改为纵向排列、input/btn 全宽 46-48px、font-size 16px（防 iOS 缩放）

- **全局 navbar 遮挡检查**（09:18）：
  - 检查所有 8 个 HTML 页面，6 个有 navbar + 容器布局
  - `style.css` 全局 `.container` padding-top 修复覆盖：index, history, game, battle_round, encyclopedia
  - `profile.html` 使用独立 `.profile-page` 类，单独修复 padding-top 为 `72px 16px 32px` + 移动端断点
  - `login.html` 无导航栏无需修复，`lobby.html` 上次已修复

- **index.html .room-header 导航栏遮挡修复**（09:59）：
  - 根因：`.room-header` 在 DOM 中位于 `<nav>` 之后、`.container` 之外，不受全局 padding-top 保护
  - 修复：`.room-header` 添加 `margin-top: 56px` 避开固定导航栏

- **profile.html 多项 bug 修复**（10:13）：
  1. **无 GSAP CDN** — 所有其他页面都有 `gsap 3.12.5` CDN，唯独 profile 缺失 → 入场动画永不执行
     → 添加 `<script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/gsap.min.js">`
  2. **navbar 显示旧数据** — `Auth.renderNav` 读 localStorage，但 `init()` 加载服务端数据后未同步
     → 在 init 中加载 profile 成功后更新 `localStorage.nickname/avatar/bio` 并重新调用 `Auth.renderNav`
  3. **navbar 无入场动画** — 其他页面都有 `gsap.from('.navbar', ...)` → 添加
  4. **logout-section 无动画** — 原 stagger 只选择 `.section-box` → 改为 `.section-box, .logout-section`
  5. **`selectedAvatar` 安全兜底** — `r.avatar` 可能为空 → `|| ''`
  6. **移除 `typeof gsap !== 'undefined'` 条件** — GSAP CDN 已加载，无需判断
  7. 添加 `cdnjs` preconnect 优化加载速度

- **用户头像迁移至 CloudBase 云存储**（10:32）：
  - `server.js`：初始化 `@cloudbase/node-sdk`，新增 `resolveAvatarUrl()` 辅助函数 + `AVATAR_CACHE` Map 缓存临时 URL
  - 上传流程：优先上传到 CloudBase 云存储（`tcbApp.uploadFile()`），存储永久 `cloud://` fileID，解析为 24h 临时 CDN URL 返回；失败则降级到本地文件系统 `/public/avatars/`
  - 旧头像清理：上传新头像前删除 CloudBase 旧文件（`tcbApp.deleteFile()`）+ 本地旧文件
  - API 端点更新：login (`/api/login`)、profile GET (`/api/profile`)、profile PUT (`/api/profile`) 三处返回 avatar 前均调用 `resolveAvatarUrl()` 解析 `cloud://` fileID 为可显示的 CDN URL
  - `profile GET` 端点加 `async`（之前遗漏）
  - `utils.js` `playerAvatarHTML()`：支持 `/` 本地路径、`https://`/`http://` CDN URL、空字符串（兜底返回空）、`cloud://` fileID（兜底返回空）四种格式
  - `profile.html`：`selectedAvatar` 判断扩展为 `/avatars/` 或 `https://` 开头；`updateProfileAvatarDisplay()` 同步适配 CDN URL
  - 角色图片 CDN URL 常量：`https://sr-mahjong-d0g8mp6w19f37dd37-1324905494.tcloudbaseapp.com`

- **CloudBase 云存储环境 ID 修复**（10:45）：
  - 错误：`server.js` 中 `TCB_ENV` 误写为 `sr-mahjong-d0g8mp6w19f37dd37-1324905494`（多了 CDN 域名后缀）
  - 正确：`sr-mahjong-d0g8mp6w19f37dd37`（通过 `envQuery` 验证）
  - 实际环境状态：体验版、上海、状态 NORMAL、存储桶 `7372-sr-mahjong-d0g8mp6w19f37dd37-1324905494`
  - `-1324905494` 后缀仅用于 CDN 域名和存储桶名称，不是环境 ID 的一部分
  - CloudBase SDK 条件初始化：仅在 `TENCENT_SECRET_ID` + `TENCENT_SECRET_KEY` 都配置时启用，否则降级本地存储
  - `.env` 中 `TENCENT_SECRET_ID` 和 `TENCENT_SECRET_KEY` 需从 [腾讯云 API 密钥](https://console.cloud.tencent.com/cam/capi) 获取

- **导航栏头像 + 上传日志修复**（10:49）：
  - `auth.js` `renderNav()` 图片判断从仅 `/avatars/` 扩展为 `/` 或 `http` 开头
  - `server.js` 上传成功新增 `console.log('✅ 头像上传成功 CloudBase: ...')` 和本地兜底日志

- **角色数据迁移到 Supabase**（10:58-11:15）：
  - 新建 `tools/migrate_characters.sql`：`characters` 表 DDL（id PK, faction, name, tier, limit, passive, innate, extra, note, image, created_at, updated_at）+ RLS 策略
  - `server.js` 新增 `loadCharactersFromDB()`：首选 Supabase，表不存在/为空时自动从本地 JSON 种数据；失败则降级 `loadCharactersFromLocal()`
  - `loadFromLocalJSON()` 新增 `loadCharactersFromLocal()` 调用
  - `getCharacterData()` / `getCharactersSync()`：简化为从 `_charCache` 读取，空则兜底本地文件
  - `/api/characters`：不再读文件+合并 imgMap，直接返回 `_charCache`
  - `/api/upload-json`：改为 `async`，同时写入 Supabase（`upsert`）和本地 JSON备份
  - **移除 CSV 上传端点** `/api/upload-csv`、`parseCSV()` 函数、`CSV_FILE`/`IMG_MAP_FILE` 常量引用
  - `character-images.json` 不再被引用（image 字段已内嵌在 characters.json/t 数据库中）
  - `sync_characters.py` 无需修改（仍写本地 JSON 作为离线备份）

- **draftPools / finishedRooms 内存数据持久化**（11:15）：
  - `draftPools`：`startPoolDraft()` 保存 `_draftPool` 到 `socketRooms[roomId]._draftPool` → `saveSocketRooms()`；每次选角后更新保存；选角完成则清除
  - 服务器重启恢复：`loadFromDB()` 加载 socket_rooms 时从 `_draftPool` 字段重建 `draftPools`
  - `finishedRooms`：`loadFromDB()` 加载 game_history 时同步填充 `finishedRooms`（从 `ended_at` 取时间戳），防止重启后重连已结束房间

- **移除所有本地存储，全面迁移到 Supabase/CloudBase**（11:07）：
  - 删除 `DATA_DIR`、`USERS_FILE`、`ROOMS_FILE`、`CHARS_FILE`、`SOCKET_ROOMS_FILE`、`DRAFT_RESULTS_FILE` 常量 + `fs.mkdirSync(DATA_DIR)` 初始化
  - 删除 `loadFromLocalJSON()` 函数（完全不再从本地 JSON 读取用户/房间/选角结果）
  - `loadFromDB()` catch 块改为纯错误提示，不再 fallback 到本地 JSON
  - 删除 `loadCharactersFromLocal()`，重构 `seedCharactersFromLocal()` 为内联读取（仅首次迁移用，之后不再读写本地文件）
  - `getCharacterData()` / `getCharactersSync()` / `/api/characters`：移除本地 fallback，仅从 `_charCache` 读取
  - `/api/upload-json`：移除 `fs.writeFileSync` 本地备份，仅写 Supabase，失败直接返回错误
  - **头像上传完全移除本地兜底**：删除 `/avatars` Express 静态路由、`public/avatars/` 目录引用、本地文件写入/清理逻辑
  - `resolveAvatarUrl()`：移除 `/avatars/` 本地路径直接返回逻辑，仅处理 `cloud://` fileID
  - 头像上传失败直接返回错误信息，不再降级到本地文件系统
  - **唯一保留的本地读**：`seedCharactersFromLocal()` 仍可读 `data/characters.json` 作为首次 Supabase 种数据来源（一次性迁移路径）

- **README.md 全面重写**（11:27）：
  - 删除所有本地 JSON/CSV/`data/` 目录引用，反映当前 Supabase + CloudBase 架构
  - 新增 **Supabase 建表 SQL** 完整脚本（8张表 + RLS 策略）
  - 新增 **CloudBase 头像存储** 配置说明
  - 新增 **Railway 部署指南**（Dockerfile + 环境变量）
  - 删除 `run.bat`/`npm.bat`/CSV 上传/`character-images.json`/`png/` 等过时引用
  - 精简项目结构到只保留当前实际文件
  - 压缩至 ~200 行（原 470 行），表格风格统一

- **对局历史改造：唯一ID/排序/搜索/回合回放**（11:37）：
  - `server.js`：新增 `roundBattleHistory` 全局变量；`voteEndRound` 在清空出战选择前自动保存当前回合数据；`saveGameHistory` 生成唯一 `game_id`（`g`+timestamp36+random36），写入 `battle_history` JSONB
  - `/api/game-history`：新增 `sort`（asc/desc）和 `search`（按 game_id/room_id 搜）参数
  - `/api/game-history-detail`：支持 `gameId` 查询（自动判断前缀 `g`），返回 `battleHistory` 数组
  - `loadFromDB`：game_history 加载时保留 `game_id`/`battle_history` 字段
  - `api.js`：`getGameHistory` 扩展 sort/search 参数；`getGameHistoryDetail` 自动按前缀区分 gameId/roomId
  - `history.html` 全面重写：工具栏（排序按钮↑↓ + 搜索输入防抖350ms）、卡片显示 game_id、详情弹窗标签切换（选角一览/回合回放）、回合导航按钮←→查看每回合每玩家出战角色快照、回合切换带 opacity+translateY 过渡动画

- **角色图鉴动画过渡**（11:37）：
  - `encyclopedia.html`：新增 `@keyframes encyFadeUp`/`encyFadeDown` 动画；`.ency-card` 添加 `.ency-enter`/`.ency-exit` 动画类
  - `renderGrid`：改为旧卡片逐一出场（0.2s staggered）→ 新卡片逐次入场（0.35s staggered），通过 `isRendering` 锁防止重复触发

- **创建 CHANGELOG.md 版本文件**（11:46）：
  - 根目录新建 `CHANGELOG.md`，按语义化版本号分段记录所有改动（v1.0.0 / v1.0.1）
  - `MEMORY.md` 新增版本管理约定：每次实质性改动后追加 CHANGELOG；仅在用户明确指令后才 `git push`

- **UI布局优化 v1.0.2**（11:51）：
  - `history.html`：刷新按钮从 section-header 移入工具栏首位置；排序按钮改为下拉式（`.sort-dropdown` + `.sort-dropdown-menu`），点击展开菜单选择"时间倒序/时间正序"，选中后自动 `loadHistory()`；工具栏排列固定为 刷新-排序下拉-搜索框-(spacer)-记录数量
  - `encyclopedia.html`：工具栏 `.toolbar` 从单行 flex 改为纵向两行——第一行筛选 chips，第二行 `.search-row` 包含搜索框 + 统计条；与目标图2格式一致
  - 新增 `CHANGELOG.md` 版本文件并写入 v1.0.0~v1.0.2 记录

- **修复排序下拉按钮 HTML 标签显示为文本**（11:58）：`sortTrigger.textContent` → `innerHTML`，`de058fe`

- **修复投票去重漏洞**（12:05）：
  - 根因：`roundEndVotes`/`gameEndVotes` 用 `socket.id` 做 key，刷新页面后 socket.id 变更，同一玩家可重复投票（4人投票变1人刷4票）
  - `voteEndRound`/`voteEndGame`：从 `socket.id` 改 `player.name`（持久标识）做投票 key；新增 `player` 查找失败容错
  - `votedNames`：从 `room.roundEndVotes[p.id]` 改为 `room.roundEndVotes[p.name]`
  - `disconnect`：清理断开玩家的投票记录（`delete roundEndVotes[playerName]` / `delete gameEndVotes[playerName]`）
  - `getRoomState`：投票计数只统计当前在线玩家（`activeNames.has(k)` 过滤），防止重连后读到旧 socket.id 投票
  - commit `572b4dd`

- **版本号回退到 v1.0.1**（12:11）：用户要求未推送时不递增版本号。将 v1.0.2/v1.0.3 CHANGELOG 内容合并到 v1.0.1，MEMORY.md 版本号还原为 v1.0.1，更新版本管理约定措辞。commit `51b1c9a`

- **对局开始时间戳 started_at**（12:13）：
  - `rooms` 和 `game_history` 新增 `started_at TIMESTAMPTZ` 列
  - 4 处游戏开始点统一设置 `rooms[roomId].startedAt`（用 `||` 保护首次、不覆盖后续）
  - `saveRooms` / `saveGameHistory` 写入 `started_at`；`loadFromDB` 加载时保留
  - README：更新 CREATE TABLE + 已有数据库 ALTER TABLE 迁移 SQL
  - commit `9552d2b`

- **修正版本号 v1.1.0 + 推送**（12:21）：CHANGELOG.md 和 MEMORY.md 版本号从 v1.0.1 修正为 v1.1.0（用户指出写错了）。commit `3e8346f` 后执行 `git push`，全部 9 个 commit 推送至 master（`9b99afd..3e8346f`）。

- **版本管理规则明确**（12:26）：用户强调不要自行 commit/push，仅在下发推送命令时执行；版本推送 commit 格式为 `vX.Y.Z - 总结内容：xxx`

- **清空所有对局数据**（12:26）：用户为自行测试对局记录功能，清空 Supabase 6 张表（`rooms`/`socket_rooms`/`draft_results`/`character_states`/`battle_selections`/`game_history`）；SQL 保留在 `tools/clear_games.sql`

- **历史记录栏位调整**（12:30）：房间号一栏改为对局时间，对局ID一栏改为房间号，对局ID仅在详情弹窗显示；`history.html` 布局 + `server.js` API 返回新增 `createdAt` 兜底字段

- **修复时间显示"未知"**（12:35）：根本原因是内存缓存用 camelCase（`endedAt`）而 API 查询 Supabase 用 snake_case（`ended_at`），内存数据读取时字段不匹配。`/api/game-history` 和 `/api/game-history-detail` 两处都加了 `||` 兼容两种命名格式。

- **添加 nodemon 热重载**（12:42）：`npm install --save-dev nodemon`，`dev` 脚本改为 `nodemon server.js`，本地开发 `npm run dev` 文件改动自动重启。README.md 同步更新本地运行说明。

- **修复排序 + 添加分页**（12:49）：`server.js` 排序 `ended_at` 同样存在 camelCase/snake_case 不兼容问题（内存数据 `endedAt` 导致 sort 日期为 Invalid Date），改为 `a.ended_at || a.endedAt || a.created_at || a.createdAt` 四重兜底。`history.html` 新增客户端分页：10条/页、上一页/下一页/页码按钮（含省略号）、切换页码平滑滚动到列表顶部、排序/搜索自动重置到第1页。

- **v1.2.1 稳定性全面修复**（13:28）：
  - GSAP 崩溃防护：所有 8 个页面 `gsap.from/to` → `Utils.safeAnimate()` 包装，CDN 失败时静默跳过不阻断后续 JS
  - `utils.js` 新增 `safeAnimate()`、`setBtnLoading()`、`resetBtn()` 三个工具函数
  - `server.js` 创建房间 `saveRooms()` 加 `await` + try-catch；加入房间 `.single()` → `.maybeSingle()`
  - 新增 `GET /api/admin/check` 轻量检测端点（不返回 403）
  - `profile.html` 管理员检测改用新端点 + Console 日志
  - 大厅按钮增加 loading/禁用反馈
  - `.env` 添加 `ADMIN_USERS=` 配置项说明
